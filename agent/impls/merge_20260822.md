# 4-way tree merge — 2026-08-22 (base = /home/kevinni/AsymGEMM-SFT-38)

Kevin directive (agent/impls/merge.md): consolidate SFT-39 / SFT-46 / SFT
(+their LlamaFactory trees) into SFT-38 as the single base. Post-08-19-merge
drift only. Executed on c14 while ALL FOUR nodes' GPUs are driver-wedged
(validation implications below).

## Git picture (decisive)
- AsymGEMM: all four trees on main_kevin; -38/-39/-46 @473ef84, SFT @5ecc16a
  (= origin tip; A's committed c12 campaign files, anchors_tmp-only). -38 is
  an ANCESTOR of every sibling => NO committed divergence. -38 fast-forwarded
  origin/main_kevin -> 5ecc16a.
- **ZERO source divergence**: full recursive working-tree diff of asym_gemm/
  csrc/ include/ tests/ setup.py scripts/ across all four trees = identical
  (only scripts/lf/archive ceiling-state dirs differed; unioned below).
- Tracked mods anywhere: only -38's agent/impls/merge.md (Kevin's directive
  edit, kept) + 3x LlamaFactory data/dataset_info.json (registry churn).
- LlamaFactory: -38/-39/SFT all @7db8f687 (dev); -46 @ebde34d3 = ANCESTOR
  (nothing unique). NOTE: 7db8f687 was the open c18 fg-segfault suspect
  ("LF@7db8f687 x -46 stack") — -38 has run this LF rev through the entire
  96G campaign (35B + Mixtral, dozens of cells) with zero segfaults, so the
  suspicion is narrowed to the -46-side stack/env, not this LF rev per se.

## Merged into -38
1. Fast-forward to 5ecc16a (brings e6a52bd's selector_err_check.py +
   bench_c2c_bw.py and A's committed stdtps96/stdz campaign files).
2. Mechanical full-tree sweep from -39/-46/SFT (script in this dir's
   manifest header; excludes .git, venvs, build products, caches,
   profiling_results symlink): **5607 files copied** (absent here),
   **1054 preserved as .sft39/.sft46/.sft40 variants** (same path, different
   content), 48632 identical, 0 errors. Full list:
   agent/impls/merge_20260822_manifest.txt. Highlights:
   - agent/anchors_tmp: all sibling campaign scripts/ledgers/logs (c11 =
     stdtps96_a1_*/a4_*/lib2 + STDTPS96_c11.md; c18 = fgprobe series +
     STDTPS96_c18.md + reuse scans; c12/SFT = a2 chains + c17 failover kit).
     Per-machine same-name files (stdtps96_lib.sh, hbm96_occupy.py,
     stdtps96_{harvest,smoke}.py/sh, status logs, tpfig_status.log,
     stdz_launch.sh) preserved as tree-suffixed variants.
   - -38's own c14-live copies renamed FIRST to stdtps96_lib.c14.sh /
     stdtps96_lib2.c14.sh / hbm96_occupy.c14.py (residual V2 chains
     re-pointed) so the ff could land A's committed plain-name versions.
   - datasets/: 3148 new run-records + 1021 per-tree variants + the
     .merged_variants store from SFT (08-19 continuity).
   - scripts/lf/archive: ceiling_{bound,confirm,probe,reconfirm}_state dirs
     (46/SFT), runs.log per-tree variants.
3. LlamaFactory: -38 already at the newest rev (7db8f687). dataset_info.json
   registries UNIONED (+18 registrations from siblings, 0 conflicts); the
   three sibling registries preserved as data/dataset_info.json.sft{39,40,46}.
   Sibling LF DATASET JSONLs deliberately NOT copied (100s of GB,
   deterministic/rebuildable, /home at 99%); they remain in the sibling
   trees; the union registry + .repair_dataset_info.py cover rebuild-on-use.
4. profiling_results: already centralized since 08-14 (all four trees
   symlink ~/env/outputs/asymlora/live) — artifacts shared by construction.

## Deliberately reconciled (verified, then deduped)
- agent/impls/merge.md sibling copies = byte-identical to committed 473ef84
  version (git history) -> removed.
- agent/reports/midterm.md (39) = byte-identical to midterm_39tree.md ->
  removed (08-19 precedent).
- SKEW_CAMPAIGN.md (39/46) = strict line-subsets of -38's committed version
  (0 unique lines) -> removed.
- Excluded as build/env products: _C*.so, *.o, stubs/_C.pyi, driver.lock,
  venvs, caches (08-19 precedent).

## Verification
- Re-sweep after merge: 0 unaccounted sibling files (the only 6 flagged are
  exactly the dedup-verified trio classes above).
- git payload: 95 files / 3.3 MB tracked (bulk sits in gitignored artifact
  paths, preserved on disk).

## Validation decision (Kevin's light-change rule)
- Source delta across trees = ZERO bytes; LF delta = registry entries only.
  => NO functional change to any runtime path for 30B / 35B / GLM-Air /
  GLM-Flash. Per the directive's own caveat, near-capacity validation runs
  are NOT warranted — and are currently impossible: all four nodes' GPUs are
  CUDA-wedged (see gb200 wedge records, 08-21/22) awaiting admin resets.
- Post-reset smoke seal recommended anyway (08-19 precedent): 30B 96k b8 T3
  anchor vs the 2775-2779 band via the asym_sft_40 container
  (asym40_enroot_run; mount the -38 tree for the merged code) — queued.
