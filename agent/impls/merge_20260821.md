# 4-way tree re-merge — 2026-08-21 (main = /home/kevinni/AsymGEMM-SFT)

Kevin directive (re-issued): keep main the superset of SFT-38/39/46.
Delta since the 2026-08-19 merge = the STDTPS standardization campaign,
whose lanes worked inside all three sibling trees (B/c11 in -39, D/c18
in -46, C14 in -38) while banking figure data into main over NFS.

## Verdicts
- **ZERO source divergence (again)**: asym_gemm/ csrc/ tests/ setup.py
  byte-identical across all four trees (recursive diff, .so excluded —
  per-machine builds). scripts/ divergence = run-state records only;
  scripts/figures plot code identical everywhere (lanes edited main's
  copies directly).
- **NO validation runs needed** per Kevin's triviality caveat: no code
  changed, only artifacts/docs moved. The campaign itself measured 30B/
  35B/122B/Mixtral/GLMs/Hunyuan at capacity THIS WEEK on this code (all
  cells in-band, cross-checks at canonical margins) — stronger evidence
  than a fresh anchor run would give.

## Merged into main
1. agent/anchors_tmp union from all three trees (+~600 files: STDTPS
   ledgers/scripts/status logs — STDTPS_CAMPAIGN.md, stdtps46_status.log,
   c14s chains, r_*.log run logs incl. pre-08-19 hy_*/fig11*/nemo1r_*
   console logs the first merge had left tree-local).
2. datasets/ union: the campaign's new-rung dataset run-records (Air
   224/288K, Flash 384K, Hunyuan 160/224K, Mixtral 160/224K, 35B
   256/384K) copied; 76 same-path records that changed since 08-19
   stashed under datasets/.merged_variants/sft{38,39,46}-20260821/.
3. SFT-46 ceiling_search_state driver/console logs (driver.lock skipped).
4. Diverged per-tree run-state preserved under scripts/lf/.merged_variants/
   (runs.log.sft{38,39,46}, validate_rows.log.sft{39,46}) and
   agent/anchors_tmp/tpfig_status.log.sft{38,46} (each tree's campaign
   appended disjoint lines to the same filename).
5. Kept main (strictly newer): SKEW_CAMPAIGN.md (+14 lines),
   agent/impls/merge.md (adds the no-validation caveat sentence),
   agent/reports/midterm.md (the 08-13 agenda; the July-9 milestone
   report remains preserved as midterm_39tree.md — verified identical
   to -39's current copy).
6. Skipped junk classes: .cache/fa4_cute_dsl JIT caches (16k files,
   regenerable), per-machine _C.so builds, .venv*/.aioenv/.pytest_cache/
   .claude locks, build/, stubs, driver.lock.

## Final audit
Residual diff after all passes = ONLY {main-ahead files, the junk
classes above, and the stashed-variant sources still differing in
place in their own trees} — zero unaccounted content. Main is a clear
superset of all four trees as of 2026-08-21.
