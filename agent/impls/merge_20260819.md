# 4-way tree merge — 2026-08-19 (main = /home/kevinni/AsymGEMM-SFT)

Kevin directive: merge SFT-38/-39/-46 into SFT (main) as the single base.
Post-08-14-merge drift only; verified and unified as follows.

## Git picture (decisive)
- All three sibling HEADs are ANCESTORS of main's HEAD (7599921, main_kevin):
  38 @204f009 (main +5), 39/46 @f93fec7 (main +10). No committed divergence.
- **ZERO source-code divergence**: asym_gemm/ csrc/ tests/ include/ setup.py
  identical across all four trees (full recursive diff).
- profiling_results/ already centralized (08-14): all four symlink to
  /home/kevinni/env/outputs/asymlora/live/profiling_results.

## Merged into main
1. SFT-39 uncommitted (the only novel tracked content, applied --3way clean):
   - scripts/lf/profile_lora_lf_test_source.sh: gpt-oss-120b -> dequantized
     bf16 local copy path (fused/gpt-oss-120b-bf16).
   - scripts/lf/run_lf_lora_sft.sh: watchdog floor for that path.
   - agent/impls/fix_dynamic_ep.md: fig-11 final ledger entry (+23 lines).
2. SFT-39 untracked: GPTOSS120B_CAMPAIGN.md + 8 gptoss120_* scripts +
   gptoss120_dequant.py + tpfig_lib_c14g.sh + fig11t3f_chain.sh
   -> agent/anchors_tmp/.
3. Artifact unions (rsync --ignore-existing, no overwrites):
   - archive/ (main 4M -> 8.7G; July campaign archives from 38/46 + 39's),
   - agent/reports/ (midterm_39tree.md, midterm_memory.md, screenshots),
   - datasets/ (7.1M -> 17M), scripts/archive/figures,
   - scripts/lf/archive/ceiling_* state dirs (46), ceiling_search_state/
     {archive, probes} (38/39).
4. ceiling_search_state ledgers that DIFFER per tree preserved as
   *.sft38/39/46.jsonl suffixed copies (state machine untouched).

## Final sweep (2nd audit pass)
- Same-path dataset run-records that DIFFER per tree (jobs.tsv, manifest,
  validation, eval_index, metrics, token_stats): sibling versions preserved
  under datasets/.merged_variants/sft{38,39,46}/<relpath> (628 files, 8 MB).
- c18 ceiling-state dirs (ceiling_search_state_{both,source}_s04-p1-dgx-02-c18)
  + probes.sft38.bak + results.sft38.bak.jsonl copied from SFT-46.
- Residual diff after all passes = ONLY {tracked files where main is ahead
  (git-verified supersession), sync.md rename, generated stubs/_C.pyi,
  driver.lock} — zero unaccounted content.

## Deliberately NOT merged (superseded or rebuildable — verified)
- Tracked files where main is simply newer (siblings clean at older HEADs):
  route_skew_probe.py, SKEW_CAMPAIGN.md, devices.md, and the two lf scripts'
  older baselines.
- agent/impls/sync.md (39/46): renamed+rewritten on main as
  local_backup.md (08-15, marked "supersedes the 08-14 version").
- agent/reports/midterm.md (39's version): byte-identical to main's
  midterm_39tree.md (already preserved by the 08-14 merge).
- Build/JIT/env products: asym_gemm/_C*.so, stubs/_C.pyi, .cache/fa4_cute_dsl
  (thousands of .o), .aioenv libs, .pytest_cache, driver.lock.

## Validation
- No runtime-affecting change for any of the 4 validation models (the only
  functional delta is a gpt-oss-120b model PATH). Per Kevin's light-change
  caveat, full near-capacity suite not warranted; smoke seal instead:
  30B 96k b8 T3 anchor on the merged tree vs the 2775-2779 tok/s band
  (this week's fresh anchors: kfa0 2725 / p7a96 2779 / kfs96 2735).
- **RESULT (mrg819, 2026-08-19, c12 GPU0, asym_sft_40 container):**
  import OK · asym_scheduler --selftest PASS (5/5) ·
  **2778 tok/s, steps 276.3/276.6 s, loss 1.702/1.692, peak/engagement
  counters identical to anchors (K1 576, fgLoraFwd 3888)** — dead center of
  the band. NO REGRESSION; merged tree sealed.
