cmake-build-*
.idea
.DS_Store
build
demo
dist
*.egg-info
*.pyc
*.log
*.ncu-rep
*.nsys-rep
*.sqlite
__pycache__/

# UV / venv
.venv*/
uv.lock

# Third-party links created by `setup.py develop`
asym_gemm/include/cute
asym_gemm/include/cutlass

# VS Code settings
/.vscode

# clangd settings
/.clang*
/.cache

# Claude Code session state
.claude/

# Generated stub files
stubs/

# Symlinks to compiled extensions
asym_gemm/*.so
extern/
external/
vendor/
submodules/
# tests/

# Local research/notes + profiling artifacts (kept on disk, not shipped)
agent/
archive/
profiling*/
datasets/
reports/
outputs/
.aioenv/

# third-party/: build submodules (cutlass, fmt), the README, and the bundled
# LlamaFactory source are tracked; the user-installed dep repos (deepspeed,
# Liger-Kernel) are not.
third-party/*
!third-party/README.md
!third-party/cutlass
!third-party/fmt
!third-party/LlamaFactory

# In-source CMake configure residue (never commit build state)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile

# Benchmark result logs
sglang_*.jsonl
*.bak*
